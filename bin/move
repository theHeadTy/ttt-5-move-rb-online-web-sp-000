#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board = Array.new(9, " ")

puts "Welcome to Tic Tac Toe!"

user_input = gets.strip
input_to_index(user_input)

move(board, board[user_input])